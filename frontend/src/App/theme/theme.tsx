import { createTheme } from "@mui/material/";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#EA5C2B",
    },
    secondary: {
      main: "#EA5C2B",
    },
    background: {
      default: "white",
    },
  },
});
